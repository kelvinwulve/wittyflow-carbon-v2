$("#filter_select,#kt_form_type").selectpicker();

// Class definition
$("#filter_date_range").daterangepicker({
  buttonClasses: " btn",
  applyClass: "btn-primary",
  cancelClass: "btn-secondary"
});
